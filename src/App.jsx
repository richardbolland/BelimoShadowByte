import RiveDisplay from './RiveDisplay';

function App() {
  return (
    // We render the RiveDisplay component which handles
    // the full-screen layout and audio logic.
    <RiveDisplay />
  );
}

export default App;